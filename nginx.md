## nginx

### 安装

```
yum install gcc c++  ##gcc环境

yum install -y pcre pcre-devel ##perl兼容的正则表达

yum install -y zlib zlib-devl ##zlib库 nginx使用zlib对http的内容进行gzib

yum install -y openssl openssl-devel ##http协议安装openssl库

```

